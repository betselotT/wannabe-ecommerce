export type FlashSalesTypes = {
  discount: string;
  image: string;
  name: string;
  currPrice: string;
  prevPrice: string;
  rating: number;
};

export type BrowseTypes = {
  image: string;
  name: string;
};

export type SellingTypes = {
  image: string;
  name: string;
  currPrice: string;
  prevPrice: string;
  rating: number;
};
